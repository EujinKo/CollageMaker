<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowNoTitle">true</item>
        <item name="android:navigationBarColor">@color/colorLightBlue</item>
    </style>

    <style name="TitleTheme">
        <item name="android:layout_gravity">center</item>
        <item name="android:layout_margin">20dp</item>
        <item name="android:textColor">@color/colorLightBlue</item>
        <item name="android:gravity">center</item>
        <item name="android:fontFamily">@font/dawning_of_a_new_day</item>
    </style>

    <style name="ImageViewTheme">
        <item name="android:scaleType">centerCrop</item>
        <item name="android:layout_width">206dp</item>
        <item name="android:layout_height">206dp</item>
    </style>

</resources>
